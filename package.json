{
  "name": "fantasy-football-new",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @ff/core && npm run build -w @ff/api && npm run build -w @ff/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --pretty false && npm run typecheck -w @ff/web",
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @ff/api",
    "dev:web": "npm run dev -w @ff/web",
    "db:migrate": "node --experimental-strip-types scripts/migrate.ts",
    "db:seed": "npm run build -w @ff/core && npm run build -w @ff/api && node --experimental-strip-types scripts/seed.ts",
    "db:reset": "node --experimental-strip-types scripts/migrate.ts --reset && npm run db:seed",
    "sync": "npm run build -w @ff/core && npm run build -w @ff/api && node --experimental-strip-types scripts/sync.ts",
    "refresh": "npm run build -w @ff/core && npm run build -w @ff/api && node --experimental-strip-types scripts/refresh.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^10.0.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
